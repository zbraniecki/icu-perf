Get data with:

```
cargo run --features bin -p icu_datagen -- --format blob --all-keys --all-locales --out ~/projects/icu-perf/data/icu4x-1.0.postcard --cldr-tag 41.0.0 --icuexport-tag release-71-1
```
